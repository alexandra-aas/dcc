%section.topper
  .topper__content
    %h1
      Dedham
      %br
      Cycle
      %br
      Club
    %p.topper__subhead
      Fostering a welcoming & safe
      %br
      bike community in Dedham.
    = link_to "Learn More", "#mission", class: 'topper__cta'
  .topper__featured-event
    - scheduled_events = data.space.events.select{ |id, event| event[:date] != nil }.sort_by{ |id, event| event[:date] }
    - future_events = scheduled_events.select{ |id, event| event[:date] >= Date.today }
    - featured_event = future_events.first.last
    = partial 'partials/components/featured_event', locals: { event: featured_event }
